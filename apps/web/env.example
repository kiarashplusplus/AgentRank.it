# ================================
# AgentRank.it Web Application
# Environment Configuration
# ================================

# --------------------------------
# Clerk Authentication (REQUIRED)
# --------------------------------
# Get these from https://dashboard.clerk.com
# Create a new application and copy the keys from the API Keys section

NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_YOUR_KEY_HERE
CLERK_SECRET_KEY=sk_test_YOUR_KEY_HERE

# Clerk Redirect URLs
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/

# --------------------------------
# Cloudflare D1 Database (REQUIRED)
# --------------------------------
# Get these from https://dash.cloudflare.com → Workers & Pages → D1
# Create a new D1 database and copy the credentials
# These are used for both drizzle-kit migrations and runtime API access

CLOUDFLARE_ACCOUNT_ID=your_account_id
CLOUDFLARE_D1_DATABASE_ID=your_database_id
CLOUDFLARE_API_TOKEN=your_api_token

# --------------------------------
# Browser-Use Engine (OPTIONAL)
# --------------------------------
# Required for "Deep Mode" scanning with visual analysis
# Start the engine with: docker-compose up -d
# If not set, deep mode will not be available

ENGINE_URL=http://localhost:8001

# --------------------------------
# AgentRank CLI Path (REQUIRED)
# --------------------------------
# Path to the AgentRank CLI for running audits
# Options:
# 1. Monorepo: ../../dist/cli/index.js (relative to this app)
# 2. Global install: agentrank (will use npx or global install)
# 3. Custom path: /path/to/agentrank/dist/cli/index.js

AGENTRANK_CLI_PATH=agentrank

# --------------------------------
# Cron Jobs (PRODUCTION)
# --------------------------------
# Secret for authenticating cron job requests
# Generate with: openssl rand -base64 32
# Add to GitHub secrets for production deployments

CRON_SECRET=your-cron-secret-here

# --------------------------------
# Rate Limiting (OPTIONAL)
# --------------------------------
# Configure anonymous user rate limits
# Default: 3 scans per 24 hours for anonymous users

ANONYMOUS_SCAN_LIMIT=3
ANONYMOUS_WINDOW_HOURS=24
